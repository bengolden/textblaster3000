<div>
  Select a number:

  <ul>
    <% @available_numbers.each do |number| %>
      <li>
        <%= link_to(number, phone_numbers_path(number: number), method: :post) %>
      </li>
    <% end %>
  </ul>
</div>